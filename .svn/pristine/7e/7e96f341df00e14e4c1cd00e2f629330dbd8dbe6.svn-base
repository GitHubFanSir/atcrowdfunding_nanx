package com.atnanx.atcrowdfunding.user.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.connection.jedis.JedisConnectionFactory;
import redis.clients.jedis.JedisPool;

@Configuration
public class JedisConfig {

   /* @Bean
    public JedisPool jedisPool(JedisConnectionFactory jedisConnectionFactory){
        // JedisPool(GenericObjectPoolConfig poolConfig, String host)
        return  new JedisPool(jedisConnectionFactory.getPoolConfig(),jedisConnectionFactory.getHostName());
    }*/
}

